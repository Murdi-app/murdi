// رسم تشغيل المطابقة — رقم واحد في المنصة كلها.
// كان ٢٬٩٠٠ اشتراكاً ربعياً مكرَّراً في أربعة ملفات، وكان مسار الدفع يأخذه من الرابط
// فيستطيع أحدهم تمرير ?amount=1. الآن: رسم عن تشغيلة واحدة، لأي مسار، من مصدر واحد.
export const MATCH_RUN_FEE = 490;

// يشتري تشغيلة واحدة — لا اشتراكاً ولا مدة. والدفع هو الإذن:
// لم يعد المسار الثاني يحتاج اعتمادك، فمن دفع عن تشغيلة شغّلها في أي مسار شاء.
export const RUNS_PER_PAYMENT = 1;

// يُخصم من أول خدمة: يجعل الرسم عربوناً لا تكلفة، وهذا ما يُقال للعميل حرفياً.
export const FEE_CREDITED = true;

/** @deprecated بقي للتوافق مع ما لم يُحدَّث بعد — استعمل MATCH_RUN_FEE */
export const MEMBERSHIP_FEE = MATCH_RUN_FEE;

export const feeText = () => MATCH_RUN_FEE.toLocaleString('en-US');

export const membershipPitch = () => ({
  price: MATCH_RUN_FEE,
  title: 'شغّل مطابقتك',
  lines: [
    'الجهات التي تنطبق شروطها عليك بالاسم، وحدود مبالغها، وما ينقصك عند كل واحدة، وطريقة التقديم إليها.',
    'الرسم عن تشغيلة واحدة، في أي مسار تختاره — تمويل أو استثمار أو طرح. لا اشتراك ولا مدة تنتهي.',
    FEE_CREDITED ? 'ويُخصم كاملاً من أول خدمة تطلبها، فهو عربون لا رسم.' : '',
  ].filter(Boolean),
});

export const creditsLabel = (n: number) =>
  n <= 0 ? 'لا تشغيلات متاحة'
  : n === 1 ? 'تشغيلة واحدة متاحة'
  : n === 2 ? 'تشغيلتان متاحتان'
  : n <= 10 ? n + ' تشغيلات متاحة'
  : n + ' تشغيلة متاحة';
