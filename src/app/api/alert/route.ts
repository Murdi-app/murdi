import { NextResponse } from 'next/server';

// ⛔ مسار معطَّل عمداً.
//
// كان مفتوحاً للعالم بلا مصادقة ولا حد معدّل، وهو:
//   • يستدعي نموذجاً في كل طلب على حساب المنصة، فحلقة واحدة تحرق الرصيد؛
//   • ويرسل بريداً عبر Resend من noreply@murdi.sa إلى عنوان يأتي في جسم الطلب،
//     مع إدراج اسم الشركة داخل HTML بلا هروب.
//   أي أن أي شخص كان يُخرج بريد تصيّد موقّعاً من نطاق murdi.sa إلى من يشاء —
//   والنتيجة إدراج النطاق في القوائم السوداء، وهو ضرر لا يُشترى إصلاحه.
//
// ولا يستدعيه أي ملف في المنصة. أُبقي هيكله ليعيد 410 بدل 404 المفاجئ،
// فإن احتيج إليه يوماً يُعاد بناؤه خلف requireAdmin وحدٍّ معدّلي.
export async function POST() {
  return NextResponse.json({ error: 'هذا المسار معطَّل' }, { status: 410 });
}
export async function GET() {
  return NextResponse.json({ error: 'هذا المسار معطَّل' }, { status: 410 });
}
